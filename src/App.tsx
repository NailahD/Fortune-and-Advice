import React from "react";
import "./App.css";
import Router from "./routes/Router";

const App: React.FC = () => {
  {
    return (
      <div>
        <Router></Router>
      </div>
    );
  }
};

export default App;
